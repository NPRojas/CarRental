'use strict'

windows.onload = init;

function init() {
    const btn = document.querySelector('button');
    btn.onclick = calculateCarRental;
}

function calculateCarRental() {
    //Get the inputs

    //convert inputs to numbers/parse floats?

    //Calculate the unknown values

    //Display the output 
}